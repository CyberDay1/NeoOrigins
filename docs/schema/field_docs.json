{
  "*": {
    "name": "Display name for this power (optional; overrides the default).",
    "description": "Tooltip/description text for this power (optional).",
    "hidden": "If true, this power is not listed on the origin screen.",
    "condition": "Only applies while this condition passes (a DSL condition object — use pick)."
  },
  "neoorigins:action_on_event": {
    "entity_action": "EntityAction run on the player as a side-effect when the configured event fires.",
    "modifier": "FloatModifier (or array) chained onto the event's value (e.g. exhaustion, regen).",
    "block_condition": "Block-position filter for block events (block_break, block_place, block_use). Ignored on other events.",
    "effect": "EFFECT_APPLIED filter: only fire for this exact mob-effect id (e.g. 'spore:mycelium_ef'). Ignored on other events.",
    "effect_tag": "EFFECT_APPLIED filter: only fire for effects in this tag (e.g. '#minecraft:harmful'). Combine with 'effect' (OR-matched). Ignored on other events.",
    "immunity_ticks": "EFFECT_APPLIED only: after a successful cleanse (action cancels the event), grant this many ticks of full immunity to the same effect id before re-rolling. 20 = 1s; default 0 (no grace)."
  },
  "neoorigins:active_ability": {
    "cooldown_ticks": "Cooldown between uses in ticks (20 = 1s); default 60.",
    "entity_action": "EntityAction tree executed on the player when the keybind fires.",
    "hunger_cost": "Food/exhaustion points consumed per successful activation; default 0.",
    "key": "Active keybind slot that triggers the ability."
  },
  "neoorigins:active_bolt": {
    "cooldown_ticks": "Cooldown between casts in ticks (20 = 1s); default 80.",
    "speed": "Velocity of the launched wind charge along the look direction; default 1.2."
  },
  "neoorigins:active_dash": {
    "allow_vertical": "If true the dash follows look pitch; if false it stays horizontal; default false.",
    "cooldown_ticks": "Cooldown between dashes in ticks (20 = 1s); default 40.",
    "power": "Magnitude of the dash velocity vector; default 1.5.",
    "set_velocity": "If true replace the player's velocity; if false add to it; default false."
  },
  "neoorigins:active_fireball": {
    "cooldown_ticks": "Cooldown between casts in ticks (20 = 1s); default 100.",
    "speed": "Velocity applied to each of the spread small fireballs; default 1.5."
  },
  "neoorigins:active_phase": {
    "cooldown_ticks": "Cooldown between phases in ticks (20 = 1s); default 40.",
    "hunger_cost": "Food/exhaustion points consumed on a successful phase; default 0.",
    "max_depth": "Maximum wall thickness in blocks the phase can pass through; default 16."
  },
  "neoorigins:active_place_block": {
    "block_id": "Block id placed at the targeted face; default minecraft:glowstone.",
    "cooldown_ticks": "Cooldown between placements in ticks (20 = 1s); default 100.",
    "hunger_cost": "Food/exhaustion points consumed on a successful placement; default 0.",
    "max_distance": "Maximum ray-pick reach in blocks for the placement surface; default 5.0."
  },
  "neoorigins:active_recall": {
    "cooldown_ticks": "Cooldown in ticks (20 = 1s) between recalls to bed/spawn; default 600."
  },
  "neoorigins:active_swap": {
    "cooldown_ticks": "Cooldown between swaps in ticks (20 = 1s); default 80.",
    "range": "Max distance in blocks to find the looked-at entity to swap with; default 20.0."
  },
  "neoorigins:active_teleport": {
    "cooldown_ticks": "Cooldown between teleports in ticks (20 = 1s); default 60.",
    "hunger_cost": "Food/exhaustion points consumed on a successful teleport; default 0.",
    "mode": "\"target\" teleports to the looked-at spot, \"random\" to a nearby safe spot; default target.",
    "range": "Maximum teleport distance in blocks; default 32.0."
  },
  "neoorigins:attribute_modifier": {
    "amount": "Numeric value applied to the attribute under the chosen operation; required.",
    "attribute": "Attribute id to modify, e.g. minecraft:generic.movement_speed or minecraft:block_interaction_range / entity_interaction_range for reach; required.",
    "equipment_condition": "Optional gate matching a worn item by id/tag in a slot; active only while worn.",
    "location_condition": "Optional gate on dimension/biome/structure; active only while there.",
    "operation": "How amount applies: add_value, add_multiplied_base, or add_multiplied_total."
  },
  "neoorigins:breath_in_fluid": {
    "drain_rate": "(legacy) Ticks between each 1-point air drain while submerged; higher = slower (20 = 1s, default).",
    "drain_interval_ticks": "Ticks between each 1-point air drain while submerged; clearer alias of drain_rate. Higher = slower drain (20 = 1s).",
    "air_loss_per_second": "Air points lost per second while submerged. Higher = faster drain. Overrides drain_interval_ticks / drain_rate when set.",
    "fluid": "Fluid that suffocates the player, \"water\" or \"lava\"; default water."
  },
  "neoorigins:condition_passive": {
    "entity_action": "EntityAction run on the player each interval when the condition passes.",
    "interval": "Ticks between condition checks (min 1; default 20).",
    "interval_ticks": "Ticks between condition checks (20 = 1s); same as interval."
  },
  "neoorigins:mob_behavior": {
    "aggression": "neutral = vanilla AI unchanged (only retaliate applies); hostile = always target the target type on sight; conditional = target a player only while every hostile_when condition holds. Default neutral.",
    "hostile_when": "List of conditions (AND-ed) tested against the PROSPECTIVE PLAYER TARGET (piglin-style, e.g. 'player not wearing gold'), not the mob. Only used when aggression = conditional; empty list behaves like hostile.",
    "retaliate": "Also fight back against whatever damaged the mob (vanilla hurt-by-target). Default true.",
    "anger_linger_ticks": "Keep the target this many ticks after the trigger stops holding, so the mob calms down gradually instead of instantly (default 200 = 10s).",
    "aggro_range": "Max distance to acquire a target (default 16.0).",
    "target_type": "Entity type id to be hostile toward; omitted = players. Conditions only apply to player targets.",
    "call_for_help": "When retaliating, alert nearby same-type mobs (vanilla pack aggro). Default false."
  },
  "neoorigins:conditional": {
    "inner_power": "Resource id of another power that is only active while this condition holds."
  },
  "neoorigins:edible_item": {
    "always_edible": "If true the item can be eaten even at full hunger (default true).",
    "fast": "If true the item is consumed with the quick eat animation.",
    "item_condition": "Optional condition an item must match (in addition to items/tags) to be edible.",
    "items": "List of item ids that become edible (matches items OR tags).",
    "meat": "If true the food counts as meat for diet/wolf interactions.",
    "nutrition": "Hunger points restored when consumed (default 4).",
    "saturation": "Saturation modifier applied on consumption (default 0.3).",
    "tags": "List of item-tag ids that become edible (matches items OR tags)."
  },
  "neoorigins:effect_immunity": {
    "effects": "List of mob-effect ids whose application to the player is cancelled."
  },
  "neoorigins:ground_slam": {
    "cooldown_ticks": "Ticks before the slam can be reused (default 120 = 6s).",
    "damage": "Damage dealt to each entity caught in the slam (default 6.0).",
    "knockback_strength": "Outward knockback pushing hit entities away (default 1.5).",
    "radius": "Blocks around the player whose entities are hit (default 6.0)."
  },
  "neoorigins:loot_pool_grant": {
    "active": "Optional display-only translation key advertising which keybind slot the power expects (e.g. 'key.use_skill_1'). Actual dispatch is via the skill-slot system.",
    "bonus_rolls": "Extra rolls added to `rolls` per activation; default 0. Mirrors vanilla loot-pool naming.",
    "cooldown": "Cooldown in ticks between activations (20 = 1s); default 0.",
    "grant_id": "Unique id tracked so the bundle is granted only once per player. Dedups the whole pool, not individual rolled stacks. Shares the starting_equipment grant attachment so a /origin reset clears both.",
    "loot_table": "ResourceLocation of the vanilla loot table to roll on activation (e.g. 'neoorigins:rewards/wood_starter'). Reuses the full vanilla loot infrastructure (weighted entries, conditions, functions, modifiers).",
    "rolls": "Number of times the table is rolled per activation; default 1. Combined with bonus_rolls; if both are zero the harness lints the power as a no-op."
  },
  "neoorigins:model_color": {
    "alpha": "Player model tint opacity, 0.0 transparent to 1.0 opaque (default 1.0).",
    "blue": "Blue channel of the player model tint, 0.0 to 1.0 (default 1.0).",
    "green": "Green channel of the player model tint, 0.0 to 1.0 (default 1.0).",
    "red": "Red channel of the player model tint, 0.0 to 1.0 (default 1.0)."
  },
  "neoorigins:modify_damage": {
    "direction": "\"in\" scales damage taken, \"out\" scales damage dealt (default in).",
    "multiplier": "Factor applied to the damage; 0.5 halves, 2.0 doubles (default 1.0).",
    "target_group": "Optional entity-group filter limiting which targets/attackers apply."
  },
  "neoorigins:modify_player_spawn": {
    "bed_override": "If true also overrides the player's bed/anchor spawn, not just respawns.",
    "biome": "Biome the resolved respawn location must be in.",
    "dimension": "Dimension the player respawns into when spawn is overridden.",
    "spawn_strategy": "How the respawn position is chosen within the dimension/biome."
  },
  "neoorigins:particle": {
    "count": "Particles spawned each emission (default 1, min 0).",
    "frequency": "Emit every N ticks (20 = 1s; default 8, min 1).",
    "offset": "[x,y,z] offset in blocks from the player to spawn at (default [0,1,0]).",
    "particle": "Particle id, or object form like {type:minecraft:dust,color,scale}.",
    "speed": "Particle motion speed passed to the spawn call (default 0.0 = still).",
    "spread": "[x,y,z] random spread radius in blocks (default [0.25,0.5,0.25])."
  },
  "neoorigins:persistent_effect": {
    "ambient": "If true the effect is ambient (fainter HUD swirl; default true).",
    "amplifier": "Effect level, 0 = level I.",
    "duration": "Refresh duration in ticks (20 = 1s); reapplied while active.",
    "effect": "Status effect id to apply for a single-effect entry.",
    "effects": "List of effect entries (effect, amplifier, ambient, show_particles, show_icon).",
    "show_icon": "If true the effect icon shows in the HUD (default true).",
    "show_particles": "If true the effect emits ambient particles (default false)."
  },
  "neoorigins:prevent_action": {
    "action": "Action blocked: fall_damage, fire, drown, freeze, sprint_food, armor_equip, swim, sleep, elytra...",
    "active_when": "When it applies: always, sneaking, not_sneaking, on_ground, not_on_ground."
  },
  "neoorigins:prevent_death": {
    "action": "Entity action run on the player each time a lethal hit is prevented.",
    "cooldown_ticks": "Ticks the power goes inert after a save (20 = 1s; default 0).",
    "damage_types": "Comma-separated damage-type filter; only matching killing blows are prevented.",
    "invert": "If true treat damage_types as a blacklist (prevent all except those).",
    "set_health": "Health the player is left at after a save (default 1.0, min 1)."
  },
  "neoorigins:resource": {
    "color": "Hex color (#RRGGBB or #AARRGGBB) for the HUD resource bar fill.",
    "label": "Display name shown on the HUD resource bar (e.g. \"Mana\").",
    "max": "Maximum value the resource can hold (default 100).",
    "max_action": "EntityAction run once when the resource rises to its max.",
    "min": "Minimum value the resource can drop to (default 0).",
    "min_action": "EntityAction run once when the resource falls to its min.",
    "regen_condition": "EntityCondition that must pass for regen to apply (default always).",
    "regen_interval": "Ticks between regen ticks (20 = 1s; default 20, min 1).",
    "regen_rate": "Amount added each regen interval; 0 disables regen (default 0).",
    "start_value": "Value the resource starts at when first granted (default max)."
  },
  "neoorigins:restrict_armor": {
    "chest": "Item or tag id barred from the chest slot; equipping it ejects the item.",
    "feet": "Item or tag id barred from the feet slot; equipping it ejects the item.",
    "head": "Item or tag id barred from the head slot; equipping it ejects the item.",
    "legs": "Item or tag id barred from the legs slot; equipping it ejects the item."
  },
  "neoorigins:starting_equipment": {
    "components": "SNBT data-component patch applied to the granted item.",
    "count": "Stack size of the granted item (default 1).",
    "enchantments": "List of {id, level} enchantments applied to the granted item.",
    "grant_id": "Unique id tracked so the item is granted only once per player.",
    "item": "Item id granted once after the player commits their origin.",
    "legacy_tag": "Apoli-style flat SNBT translated into data components on the item.",
    "stacks": "Plural multi-item shape (v2.1.6): array of {item, count, enchantments, legacy_tag, components} entries granted in order; takes precedence over the singular root fields when non-empty."
  },
  "neoorigins:tick_action": {
    "action_type": "Periodic action: teleport_on_damage or none (default none).",
    "interval": "Ticks between each action run (20 = 1s; default 20)."
  },
  "neoorigins:tidal_wave": {
    "cone_angle": "Total width in degrees of the forward damage cone (default 60).",
    "cooldown_ticks": "Ticks before the wave can be reused (100 = 5s).",
    "damage": "Damage dealt to each entity in the cone (half-hearts; default 4).",
    "hunger_cost": "Hunger consumed per use (default 0).",
    "knockback_strength": "Forward push applied to hit entities (default 2).",
    "range": "Max reach of the water cone in blocks (default 8)."
  },
  "neoorigins:toggle": {
    "default_value": "Initial boolean value before the toggle is first flipped (default false).",
    "key": "Identifier other powers read/flip this named boolean by."
  }
}
