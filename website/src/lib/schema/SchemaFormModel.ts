// TODO: Port of `SchemaFormModel.java` from the Java side
// (src/main/java/com/cyberday1/neoorigins/power/schemaform/SchemaFormModel.java).
//
// Walks power.schema.json's discriminated `oneOf` branches keyed by `$comment`
// and produces a list of FormFieldSpec entries for the active power type.
//
// Next agent: this is the hardest single piece of the editor — see
// planning/web_editor_scope.md section 1 for context.

export {};
