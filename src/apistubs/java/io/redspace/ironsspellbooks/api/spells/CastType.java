package io.redspace.ironsspellbooks.api.spells;

/**
 * Compile-only API stub for Iron's Spells' {@code CastType} enum. Minimal
 * signature stand-in (see {@link AbstractSpell}); never bundled, real enum loads
 * at runtime when {@code irons_spellbooks} is present.
 */
public enum CastType {
    NONE,
    INSTANT,
    LONG,
    CONTINUOUS
}
